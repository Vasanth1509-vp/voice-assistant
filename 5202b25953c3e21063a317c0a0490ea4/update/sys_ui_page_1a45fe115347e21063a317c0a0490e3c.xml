<?xml version="1.0" encoding="UTF-8"?><record_update table="sys_ui_page">
    <sys_ui_page action="INSERT_OR_UPDATE">
        <category>general</category>
        <client_script/>
        <description>Voice Assistant UI Page</description>
        <direct>true</direct>
        <endpoint>x_941497_voice_ast_Voice Assistant.do</endpoint>
        <html><![CDATA[<style>
    * {
        margin: 0;
        padding: 0;
        box-sizing: border-box;
    }

    body {
        font-family: 'Inter', 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
        background: linear-gradient(135deg, #667eea 0%, #764ba2 50%, #f093fb 100%);
        min-height: 100vh;
        overflow-x: hidden;
    }

    /* Animated background particles */
    .background-animation {
        position: fixed;
        top: 0;
        left: 0;
        width: 100%;
        height: 100%;
        z-index: -1;
        opacity: 0.1;
    }

    .particle {
        position: absolute;
        background: white;
        border-radius: 50%;
        animation: float 6s ease-in-out infinite;
    }

    .particle:nth-child(1) { width: 4px; height: 4px; left: 10%; animation-delay: 0s; }
    .particle:nth-child(2) { width: 6px; height: 6px; left: 20%; animation-delay: -2s; }
    .particle:nth-child(3) { width: 3px; height: 3px; left: 30%; animation-delay: -4s; }
    .particle:nth-child(4) { width: 5px; height: 5px; left: 40%; animation-delay: -1s; }
    .particle:nth-child(5) { width: 4px; height: 4px; left: 50%; animation-delay: -3s; }
    .particle:nth-child(6) { width: 6px; height: 6px; left: 60%; animation-delay: -5s; }
    .particle:nth-child(7) { width: 3px; height: 3px; left: 70%; animation-delay: -2.5s; }
    .particle:nth-child(8) { width: 5px; height: 5px; left: 80%; animation-delay: -1.5s; }
    .particle:nth-child(9) { width: 4px; height: 4px; left: 90%; animation-delay: -0.5s; }

    @keyframes float {
        0%, 100% { transform: translateY(100vh) rotate(0deg); opacity: 0; }
        10% { opacity: 1; }
        90% { opacity: 1; }
        100% { transform: translateY(-10vh) rotate(360deg); opacity: 0; }
    }

    .voice-assistant-wrapper {
        display: flex;
        flex-direction: column;
        align-items: center;
        padding: 20px;
        min-height: 100vh;
    }

    .container {
        background: rgba(255, 255, 255, 0.95);
        backdrop-filter: blur(20px);
        border: 1px solid rgba(255, 255, 255, 0.2);
        border-radius: 24px;
        box-shadow: 0 32px 64px rgba(0, 0, 0, 0.15);
        padding: 40px;
        max-width: 900px;
        width: 100%;
        margin: 20px;
        position: relative;
        overflow: hidden;
    }

    .container::before {
        content: '';
        position: absolute;
        top: 0;
        left: 0;
        right: 0;
        height: 1px;
        background: linear-gradient(90deg, transparent, rgba(255,255,255,0.8), transparent);
        animation: shimmer 3s ease-in-out infinite;
    }

    @keyframes shimmer {
        0%, 100% { opacity: 0; }
        50% { opacity: 1; }
    }

    .header {
        text-align: center;
        margin-bottom: 40px;
        position: relative;
    }

    .header h1 {
        background: linear-gradient(135deg, #667eea, #764ba2);
        -webkit-background-clip: text;
        -webkit-text-fill-color: transparent;
        background-clip: text;
        font-size: 3em;
        margin-bottom: 10px;
        font-weight: 700;
        letter-spacing: -1px;
    }

    .header p {
        color: #666;
        font-size: 1.2em;
        opacity: 0.8;
        font-weight: 400;
    }

    .voice-controls {
        display: flex;
        justify-content: center;
        gap: 20px;
        margin-bottom: 30px;
        position: relative;
    }

    .voice-btn {
        background: linear-gradient(135deg, #ff6b6b, #ee5a24);
        border: none;
        border-radius: 50%;
        width: 90px;
        height: 90px;
        color: white;
        font-size: 1.8em;
        cursor: pointer;
        transition: all 0.4s cubic-bezier(0.4, 0, 0.2, 1);
        display: flex;
        align-items: center;
        justify-content: center;
        position: relative;
        box-shadow: 0 8px 32px rgba(255, 107, 107, 0.3);
    }

    .voice-btn::before {
        content: '';
        position: absolute;
        inset: -2px;
        background: linear-gradient(45deg, #ff6b6b, #ee5a24, #ff6b6b);
        border-radius: 50%;
        z-index: -1;
        opacity: 0;
        transition: opacity 0.3s ease;
    }

    .voice-btn:hover {
        transform: scale(1.1) translateY(-2px);
        box-shadow: 0 16px 48px rgba(255, 107, 107, 0.4);
    }

    .voice-btn:hover::before {
        opacity: 1;
        animation: rotate 2s linear infinite;
    }

    .voice-btn.listening {
        background: linear-gradient(135deg, #26de81, #20bf6b);
        box-shadow: 0 8px 32px rgba(38, 222, 129, 0.4);
        animation: listening-pulse 2s ease-in-out infinite;
    }

    .voice-btn.listening::after {
        content: '';
        position: absolute;
        inset: -10px;
        border: 2px solid rgba(38, 222, 129, 0.3);
        border-radius: 50%;
        animation: listening-ring 2s ease-in-out infinite;
    }

    @keyframes listening-pulse {
        0%, 100% { transform: scale(1); }
        50% { transform: scale(1.05); }
    }

    @keyframes listening-ring {
        0% { transform: scale(1); opacity: 1; }
        100% { transform: scale(1.5); opacity: 0; }
    }

    @keyframes rotate {
        from { transform: rotate(0deg); }
        to { transform: rotate(360deg); }
    }

    .status {
        text-align: center;
        padding: 20px 24px;
        background: rgba(248, 249, 250, 0.8);
        backdrop-filter: blur(10px);
        border-radius: 16px;
        margin-bottom: 24px;
        border: 1px solid rgba(255, 255, 255, 0.3);
        font-weight: 500;
        position: relative;
        overflow: hidden;
    }

    .status.listening {
        background: rgba(38, 222, 129, 0.1);
        border-color: rgba(38, 222, 129, 0.3);
        color: #20bf6b;
    }

    .status.processing {
        background: rgba(116, 185, 255, 0.1);
        border-color: rgba(116, 185, 255, 0.3);
        color: #0984e3;
    }

    .chat-container {
        max-height: 450px;
        overflow-y: auto;
        border: 1px solid rgba(221, 221, 221, 0.3);
        border-radius: 16px;
        padding: 24px;
        margin-bottom: 24px;
        background: rgba(250, 250, 250, 0.5);
        backdrop-filter: blur(10px);
        scroll-behavior: smooth;
    }

    .chat-container::-webkit-scrollbar {
        width: 6px;
    }

    .chat-container::-webkit-scrollbar-track {
        background: rgba(0, 0, 0, 0.05);
        border-radius: 3px;
    }

    .chat-container::-webkit-scrollbar-thumb {
        background: rgba(0, 0, 0, 0.2);
        border-radius: 3px;
    }

    .message {
        margin-bottom: 16px;
        padding: 16px 20px;
        border-radius: 18px;
        max-width: 85%;
        position: relative;
        animation: messageSlideIn 0.5s ease-out;
        word-wrap: break-word;
        line-height: 1.5;
    }

    @keyframes messageSlideIn {
        from { opacity: 0; transform: translateY(10px); }
        to { opacity: 1; transform: translateY(0); }
    }

    .user-message {
        background: linear-gradient(135deg, #007bff, #0056b3);
        color: white;
        margin-left: auto;
        text-align: right;
        box-shadow: 0 4px 16px rgba(0, 123, 255, 0.3);
    }

    .user-message::before {
        content: ']]>👤<![CDATA[';
        position: absolute;
        right: -8px;
        top: -8px;
        background: white;
        border-radius: 50%;
        width: 24px;
        height: 24px;
        display: flex;
        align-items: center;
        justify-content: center;
        font-size: 12px;
        box-shadow: 0 2px 8px rgba(0, 0, 0, 0.1);
    }

    .assistant-message {
        background: linear-gradient(135deg, #e9ecef, #f8f9fa);
        color: #333;
        border: 1px solid rgba(0, 0, 0, 0.05);
        box-shadow: 0 2px 12px rgba(0, 0, 0, 0.05);
    }

    .assistant-message::before {
        content: ']]>🤖<![CDATA[';
        position: absolute;
        left: -8px;
        top: -8px;
        background: linear-gradient(135deg, #667eea, #764ba2);
        border-radius: 50%;
        width: 24px;
        height: 24px;
        display: flex;
        align-items: center;
        justify-content: center;
        font-size: 12px;
        box-shadow: 0 2px 8px rgba(0, 0, 0, 0.1);
    }

    .typing-indicator {
        display: flex;
        align-items: center;
        gap: 8px;
        color: #666;
        font-style: italic;
        margin-bottom: 16px;
    }

    .typing-dots {
        display: flex;
        gap: 4px;
    }

    .typing-dot {
        width: 6px;
        height: 6px;
        background: #666;
        border-radius: 50%;
        animation: typing 1.4s ease-in-out infinite;
    }

    .typing-dot:nth-child(1) { animation-delay: 0.2s; }
    .typing-dot:nth-child(2) { animation-delay: 0.4s; }
    .typing-dot:nth-child(3) { animation-delay: 0.6s; }

    @keyframes typing {
        0%, 60%, 100% { transform: translateY(0); opacity: 0.4; }
        30% { transform: translateY(-10px); opacity: 1; }
    }

    .actions-grid {
        display: grid;
        grid-template-columns: repeat(auto-fit, minmax(200px, 1fr));
        gap: 16px;
        margin-top: 32px;
    }

    .action-card {
        background: linear-gradient(135deg, #74b9ff, #0984e3);
        color: white;
        padding: 24px;
        border-radius: 16px;
        text-align: center;
        cursor: pointer;
        transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1);
        position: relative;
        overflow: hidden;
        border: 1px solid rgba(255, 255, 255, 0.1);
    }

    .action-card::before {
        content: '';
        position: absolute;
        top: 0;
        left: -100%;
        width: 100%;
        height: 100%;
        background: linear-gradient(90deg, transparent, rgba(255, 255, 255, 0.2), transparent);
        transition: left 0.5s ease;
    }

    .action-card:hover::before {
        left: 100%;
    }

    .action-card:hover {
        transform: translateY(-8px) scale(1.02);
        box-shadow: 0 20px 40px rgba(116, 185, 255, 0.4);
    }

    .action-card h3 {
        margin-bottom: 12px;
        font-size: 1.3em;
        font-weight: 600;
    }

    .action-card p {
        font-size: 0.95em;
        opacity: 0.9;
        line-height: 1.4;
    }

    .transcript {
        background: rgba(255, 255, 255, 0.8);
        backdrop-filter: blur(10px);
        border: 1px solid rgba(221, 221, 221, 0.3);
        border-radius: 16px;
        padding: 20px;
        min-height: 120px;
        margin-top: 24px;
        position: relative;
    }

    .transcript strong {
        color: #333;
        font-weight: 600;
        display: block;
        margin-bottom: 12px;
        font-size: 1.1em;
    }

    #transcriptText {
        font-family: 'Monaco', 'Menlo', 'Ubuntu Mono', monospace;
        background: rgba(248, 249, 250, 0.8);
        padding: 12px 16px;
        border-radius: 8px;
        border: 1px solid rgba(0, 0, 0, 0.05);
        min-height: 60px;
        display: flex;
        align-items: center;
        color: #555;
        line-height: 1.4;
    }

    .error {
        color: #dc3545;
        background: linear-gradient(135deg, #f8d7da, #f5c6cb);
        border: 1px solid rgba(220, 53, 69, 0.3);
        padding: 16px 20px;
        border-radius: 12px;
        margin: 12px 0;
        box-shadow: 0 4px 16px rgba(220, 53, 69, 0.1);
        animation: messageSlideIn 0.5s ease-out;
    }

    .info {
        color: #0c5460;
        background: linear-gradient(135deg, #d1ecf1, #bee5eb);
        border: 1px solid rgba(12, 84, 96, 0.3);
        padding: 16px 20px;
        border-radius: 12px;
        margin: 12px 0;
        box-shadow: 0 4px 16px rgba(12, 84, 96, 0.1);
        animation: messageSlideIn 0.5s ease-out;
    }

    .loading-spinner {
        display: inline-block;
        width: 20px;
        height: 20px;
        border: 2px solid rgba(116, 185, 255, 0.3);
        border-top: 2px solid #74b9ff;
        border-radius: 50%;
        animation: spin 0.8s linear infinite;
        margin-right: 8px;
    }

    @keyframes spin {
        0% { transform: rotate(0deg); }
        100% { transform: rotate(360deg); }
    }

    /* Voice activity indicator */
    .voice-activity {
        position: absolute;
        bottom: -15px;
        left: 50%;
        transform: translateX(-50%);
        display: flex;
        gap: 2px;
        opacity: 0;
        transition: opacity 0.3s ease;
    }

    .voice-activity.active {
        opacity: 1;
    }

    .voice-bar {
        width: 3px;
        height: 6px;
        background: rgba(38, 222, 129, 0.8);
        border-radius: 2px;
        animation: voice-bars 0.8s ease-in-out infinite;
    }

    .voice-bar:nth-child(1) { animation-delay: 0.1s; }
    .voice-bar:nth-child(2) { animation-delay: 0.2s; }
    .voice-bar:nth-child(3) { animation-delay: 0.3s; }
    .voice-bar:nth-child(4) { animation-delay: 0.4s; }
    .voice-bar:nth-child(5) { animation-delay: 0.5s; }

    @keyframes voice-bars {
        0%, 100% { height: 6px; }
        50% { height: 20px; }
    }

    /* Responsive design */
    @media (max-width: 768px) {
        .container {
            margin: 10px;
            padding: 24px;
        }
        
        .header h1 {
            font-size: 2.2em;
        }
        
        .voice-btn {
            width: 80px;
            height: 80px;
            font-size: 1.6em;
        }
        
        .actions-grid {
            grid-template-columns: 1fr;
            gap: 12px;
        }
        
        .message {
            max-width: 95%;
        }
    }

    /* Theme toggle button */
    .theme-toggle {
        position: absolute;
        top: 20px;
        right: 20px;
        background: rgba(255, 255, 255, 0.2);
        border: 1px solid rgba(255, 255, 255, 0.3);
        border-radius: 12px;
        padding: 8px 12px;
        cursor: pointer;
        color: white;
        font-size: 1.1em;
        transition: all 0.3s ease;
        backdrop-filter: blur(10px);
    }

    .theme-toggle:hover {
        background: rgba(255, 255, 255, 0.3);
        transform: translateY(-2px);
    }
</style>

<!-- Background Animation -->
<div class="background-animation">
    <div class="particle"></div>
    <div class="particle"></div>
    <div class="particle"></div>
    <div class="particle"></div>
    <div class="particle"></div>
    <div class="particle"></div>
    <div class="particle"></div>
    <div class="particle"></div>
    <div class="particle"></div>
</div>

<div class="voice-assistant-wrapper">
    <div class="theme-toggle" onclick="toggleTheme()">]]>🌙<![CDATA[</div>
    
    <div class="container">
        <div class="header">
            <h1>]]>🎙<![CDATA[️ ServiceNow Voice Assistant</h1>
            <p>Your intelligent AI-powered ServiceNow companion</p>
        </div>

        <div class="voice-controls">
            <button id="startBtn" class="voice-btn" title="Start Listening">]]>🎤<![CDATA[</button>
            <button id="stopBtn" class="voice-btn" title="Stop Listening" style="display:none;">⏹️</button>
            
            <!-- Voice activity indicator -->
            <div class="voice-activity" id="voiceActivity">
                <div class="voice-bar"></div>
                <div class="voice-bar"></div>
                <div class="voice-bar"></div>
                <div class="voice-bar"></div>
                <div class="voice-bar"></div>
            </div>
        </div>

        <div id="status" class="status">
            <span class="loading-spinner" id="loadingSpinner" style="display:none;"></span>
            <span id="statusText">Ready to listen. Click the microphone to start.</span>
        </div>

        <div class="chat-container" id="chatContainer">
            <div class="message assistant-message" id="welcomeMessage">
                Hello! I'm your ServiceNow Voice Assistant. I can help you with:
                <br/>• Checking ticket status
                <br/>• Viewing knowledge base articles
                <br/>• Accessing catalog forms
                <br/>• Managing user profiles
                <br/>• Creating incidents
                <br/><br/>Just speak your request!
            </div>
        </div>

        <div class="actions-grid">
            <div class="action-card" onclick="handleQuickAction('ticket')">
                <h3>]]>🎫<![CDATA[ Ticket Status</h3>
                <p>Check the status and details of your ServiceNow tickets</p>
            </div>
            <div class="action-card" onclick="handleQuickAction('knowledge')">
                <h3>]]>📚<![CDATA[ Knowledge Base</h3>
                <p>Search articles and documentation for instant answers</p>
            </div>
            <div class="action-card" onclick="handleQuickAction('catalog')">
                <h3>]]>📋<![CDATA[ Service Catalog</h3>
                <p>Browse and request services from the catalog</p>
            </div>
            <div class="action-card" onclick="handleQuickAction('incident')">
                <h3>]]>🚨<![CDATA[ Create Incident</h3>
                <p>Report new incidents and technical issues</p>
            </div>
        </div>

        <div class="transcript" id="transcript">
            <strong>]]>🎵<![CDATA[ Voice Transcript</strong>
            <div id="transcriptText">Waiting for voice input...</div>
        </div>
    </div>
</div>

<script>
    class ServiceNowVoiceAssistant {
        constructor() {
            this.recognition = null;
            this.isListening = false;
            this.conversationHistory = [];
            this.isDarkTheme = false;
            
            // ServiceNow User Context
            this.currentUser = {
                sysId: null,
                userName: null,
                firstName: null,
                lastName: null,
                email: null
            };
            
            this.initializeUser();
            this.initializeSpeechRecognition();
            this.initializeEventListeners();
            this.initializeServiceNowContext();
        }

        async initializeUser() {
            // Method 1: Try g_user (standard ServiceNow)
            if (typeof g_user !== 'undefined') {
                if (g_user) {
                    this.currentUser.sysId = g_user.userID;
                    this.currentUser.userName = g_user.userName;
                    this.currentUser.firstName = g_user.firstName;
                    this.currentUser.lastName = g_user.lastName;
                    this.currentUser.email = g_user.email;
                    console.log('User detected via g_user:', this.currentUser.userName);
                    return;
                }
            }

            // Method 2: Try NOW object (newer ServiceNow versions)
            if (typeof NOW !== 'undefined') {
                if (NOW.user) {
                    this.currentUser.sysId = NOW.user.userID;
                    this.currentUser.userName = NOW.user.userName;
                    this.currentUser.firstName = NOW.user.firstName;
                    this.currentUser.lastName = NOW.user.lastName;
                    this.currentUser.email = NOW.user.email;
                    console.log('User detected via NOW.user:', this.currentUser.userName);
                    return;
                }
            }

            // Method 3: Try to get user info via API call
            try {
                await this.getUserFromAPI();
            } catch (error) {
                console.log('Could not get user from API:', error.message);
            }

            // Method 4: Check if we're in ServiceNow by trying a simple API call
            if (!this.currentUser.sysId) {
                try {
                    var testResponse = await this.makeServiceNowRequest('sys_user', 'sys_id!=null', 'sys_id,user_name,first_name,last_name,email', 'GET', null, 1);
                    if (testResponse.result) {
                        if (testResponse.result.length > 0) {
                            var user = testResponse.result[0];
                            this.currentUser.sysId = user.sys_id;
                            this.currentUser.userName = user.user_name;
                            this.currentUser.firstName = user.first_name;
                            this.currentUser.lastName = user.last_name;
                            this.currentUser.email = user.email;
                            console.log('Using API fallback user:', this.currentUser.userName);
                            
                            this.addInfoMessage(']]>🔧<![CDATA[ Note: Using API fallback for user context. Some features may be limited.');
                        }
                    }
                } catch (error) {
                    console.log('ServiceNow API test failed:', error.message);
                }
            }
        }

        async getUserFromAPI() {
            var response = await this.makeServiceNowRequest('sys_user', 'user_name=javascript:gs.getUserName()', 'sys_id,user_name,first_name,last_name,email');
            if (response.result) {
                if (response.result.length > 0) {
                    var user = response.result[0];
                    this.currentUser.sysId = user.sys_id;
                    this.currentUser.userName = user.user_name;
                    this.currentUser.firstName = user.first_name;
                    this.currentUser.lastName = user.last_name;
                    this.currentUser.email = user.email;
                    console.log('User detected via API:', this.currentUser.userName);
                }
            }
        }

        initializeSpeechRecognition() {
            var speechRecognitionSupported = false;
            var SpeechRecognition = null;
            
            if ('webkitSpeechRecognition' in window) {
                SpeechRecognition = window.webkitSpeechRecognition;
                speechRecognitionSupported = true;
            }
            if ('SpeechRecognition' in window) {
                SpeechRecognition = window.SpeechRecognition;
                speechRecognitionSupported = true;
            }
            
            if (speechRecognitionSupported) {
                this.recognition = new SpeechRecognition();
                this.recognition.continuous = true;
                this.recognition.interimResults = true;
                this.recognition.lang = 'en-US';
                
                var self = this;
                
                this.recognition.onstart = function() {
                    self.updateStatus(']]>🎤<![CDATA[ Listening... Speak now!', 'listening');
                    self.isListening = true;
                    self.toggleButtons();
                    self.showVoiceActivity(true);
                };
                
                this.recognition.onresult = function(event) {
                    var finalTranscript = '';
                    var interimTranscript = '';
                    
                    var startIndex = event.resultIndex;
                    var resultsLength = event.results.length;
                    
                    for (var i = startIndex; i !== resultsLength; i++) {
                        if (i >= resultsLength) break;
                        
                        var transcript = event.results[i][0].transcript;
                        if (event.results[i].isFinal) {
                            finalTranscript += transcript;
                        } else {
                            interimTranscript += transcript;
                        }
                        
                        i++;
                        if (i >= resultsLength) break;
                    }
                    
                    self.updateTranscript(finalTranscript + interimTranscript);
                    
                    if (finalTranscript) {
                        self.processVoiceCommand(finalTranscript.trim());
                    }
                };
                
                this.recognition.onerror = function(event) {
                    self.handleError(']]>🚫<![CDATA[ Speech recognition error: ' + event.error);
                    self.showVoiceActivity(false);
                };
                
                this.recognition.onend = function() {
                    self.updateStatus('✨ Voice recognition stopped.', 'idle');
                    self.isListening = false;
                    self.toggleButtons();
                    self.showVoiceActivity(false);
                };
            } else {
                this.handleError(']]>🚫<![CDATA[ Speech recognition not supported in this browser.');
            }
        }

        initializeEventListeners() {
            var self = this;
            document.getElementById('startBtn').addEventListener('click', function() {
                self.startListening();
            });
            document.getElementById('stopBtn').addEventListener('click', function() {
                self.stopListening();
            });
        }

        initializeServiceNowContext() {
            if (this.currentUser.firstName) {
                var welcomeElement = document.getElementById('welcomeMessage');
                if (welcomeElement) {
                    welcomeElement.innerHTML = 
                        ']]>👋<![CDATA[ Hello ' + this.currentUser.firstName + '! I\'m your ServiceNow Voice Assistant. I can help you with:' +
                        '<br/>• ]]>🎫<![CDATA[ Checking your ticket status' +
                        '<br/>• ]]>📚<![CDATA[ Viewing knowledge base articles' +
                        '<br/>• ]]>📋<![CDATA[ Browsing the service catalog' +
                        '<br/>• ]]>👤<![CDATA[ Managing your profile' +
                        '<br/>• ]]>🚨<![CDATA[ Creating new incidents' +
                        '<br/><br/>✨ Just speak your request or click one of the action cards below!';
                }
            }
            
            console.log(']]>🎙<![CDATA[️ ServiceNow Voice Assistant initialized for user:', this.currentUser.userName);
        }

        showVoiceActivity(show) {
            var voiceActivity = document.getElementById('voiceActivity');
            if (show) {
                voiceActivity.classList.add('active');
            } else {
                voiceActivity.classList.remove('active');
            }
        }

        showLoadingSpinner(show) {
            var spinner = document.getElementById('loadingSpinner');
            if (show) {
                spinner.style.display = 'inline-block';
            } else {
                spinner.style.display = 'none';
            }
        }

        showTypingIndicator() {
            var chatContainer = document.getElementById('chatContainer');
            var typingDiv = document.createElement('div');
            typingDiv.className = 'typing-indicator';
            typingDiv.id = 'typingIndicator';
            typingDiv.innerHTML = ']]>🤖<![CDATA[ Assistant is typing<div class="typing-dots"><div class="typing-dot"></div><div class="typing-dot"></div><div class="typing-dot"></div></div>';
            chatContainer.appendChild(typingDiv);
            chatContainer.scrollTop = chatContainer.scrollHeight;
        }

        hideTypingIndicator() {
            var typingIndicator = document.getElementById('typingIndicator');
            if (typingIndicator) {
                typingIndicator.remove();
            }
        }

        async makeServiceNowRequest(table, query, fields, method, data, limit) {
            if (!query) query = '';
            if (!fields) fields = '';
            if (!method) method = 'GET';
            if (!limit) limit = 10;
            
            try {
                var url = '/api/now/table/' + table;
                var params = new URLSearchParams();
                
                if (query) params.append('sysparm_query', query);
                if (fields) params.append('sysparm_fields', fields);
                if (method === 'GET') params.append('sysparm_limit', limit.toString());
                
                if (params.toString()) {
                    url += '?' + params.toString();
                }
                
                var options = {
                    method: method,
                    headers: {
                        'Content-Type': 'application/json',
                        'Accept': 'application/json',
                        'X-UserToken': window.g_ck || ''
                    },
                    credentials: 'same-origin'
                };
                
                var isPostMethod = (method === 'POST');
                var isPutMethod = (method === 'PUT');
                var isPatchMethod = (method === 'PATCH');
                
                if (data) {
                    if (isPostMethod || isPutMethod || isPatchMethod) {
                        options.body = JSON.stringify(data);
                    }
                }
                
                var response = await fetch(url, options);
                
                if (!response.ok) {
                    throw new Error('ServiceNow API Error: ' + response.status + ' ' + response.statusText);
                }
                
                return await response.json();
            } catch (error) {
                console.error('ServiceNow API Request Failed:', error);
                throw error;
            }
        }

        startListening() {
            if (this.recognition) {
                if (!this.isListening) {
                    this.recognition.start();
                }
            }
        }

        stopListening() {
            if (this.recognition) {
                if (this.isListening) {
                    this.recognition.stop();
                }
            }
        }

        toggleButtons() {
            var startBtn = document.getElementById('startBtn');
            var stopBtn = document.getElementById('stopBtn');
            
            if (this.isListening) {
                startBtn.style.display = 'none';
                stopBtn.style.display = 'flex';
                startBtn.classList.add('listening');
            } else {
                startBtn.style.display = 'flex';
                stopBtn.style.display = 'none';
                startBtn.classList.remove('listening');
            }
        }

        updateStatus(message, type) {
            if (!type) type = 'idle';
            var statusDiv = document.getElementById('status');
            var statusText = document.getElementById('statusText');
            statusText.textContent = message;
            statusDiv.className = 'status ' + type;
            
            if (type === 'processing') {
                this.showLoadingSpinner(true);
            } else {
                this.showLoadingSpinner(false);
            }
        }

        updateTranscript(text) {
            var defaultText = 'Waiting for voice input...';
            document.getElementById('transcriptText').textContent = text || defaultText;
        }

        addMessage(message, isUser) {
            var chatContainer = document.getElementById('chatContainer');
            var messageDiv = document.createElement('div');
            var messageClass = isUser ? 'user-message' : 'assistant-message';
            messageDiv.className = 'message ' + messageClass;
            messageDiv.textContent = message;
            chatContainer.appendChild(messageDiv);
            chatContainer.scrollTop = chatContainer.scrollHeight;
        }

        addInfoMessage(message) {
            var chatContainer = document.getElementById('chatContainer');
            var infoDiv = document.createElement('div');
            infoDiv.className = 'info';
            infoDiv.textContent = message;
            chatContainer.appendChild(infoDiv);
            chatContainer.scrollTop = chatContainer.scrollHeight;
        }

        async processVoiceCommand(command) {
            this.addMessage(command, true);
            this.updateStatus(']]>🤖<![CDATA[ Processing your request...', 'processing');
            this.showTypingIndicator();
            
            this.storeConversation(command, 'user');
            
            // Add a small delay for better UX
            await new Promise(resolve => setTimeout(resolve, 800));
            
            var response = await this.generateResponse(command);
            this.hideTypingIndicator();
            this.addMessage(response, false);
            this.storeConversation(response, 'assistant');
            
            this.speakResponse(response);
            
            this.updateStatus('✨ Ready to listen. Click the microphone to start.', 'idle');
        }

        async generateResponse(command) {
            var lowerCommand = command.toLowerCase();
            
            var isTicketQuery = (lowerCommand.indexOf('ticket') !== -1);
            var isStatusQuery = (lowerCommand.indexOf('status') !== -1);
            var isKnowledgeQuery = (lowerCommand.indexOf('knowledge') !== -1);
            var isArticleQuery = (lowerCommand.indexOf('article') !== -1);
            var isCatalogQuery = (lowerCommand.indexOf('catalog') !== -1);
            var isServiceQuery = (lowerCommand.indexOf('service') !== -1);
            var isIncidentQuery = (lowerCommand.indexOf('incident') !== -1);
            var isCreateQuery = (lowerCommand.indexOf('create') !== -1);
            var isReportQuery = (lowerCommand.indexOf('report') !== -1);
            var isProfileQuery = (lowerCommand.indexOf('profile') !== -1);
            var isUserQuery = (lowerCommand.indexOf('user') !== -1);
            
            if (isTicketQuery || isStatusQuery) {
                return await this.handleTicketQuery(command);
            }
            if (isKnowledgeQuery || isArticleQuery) {
                return await this.handleKnowledgeQuery(command);
            }
            if (isCatalogQuery || isServiceQuery) {
                return await this.handleCatalogQuery(command);
            }
            if (isIncidentQuery || isCreateQuery || isReportQuery) {
                return await this.handleIncidentCreation(command);
            }
            if (isProfileQuery || isUserQuery) {
                return await this.handleUserProfile(command);
            }
            
            return "✨ I can help you with ticket status, knowledge articles, service catalog, incident creation, and user profiles. What would you like me to assist you with?";
        }

        async handleTicketQuery(command) {
            try {
                if (!this.currentUser.sysId) {
                    return "]]>📋<![CDATA[ I can show you general ticket information. For personalized results, please ensure you're logged into ServiceNow properly.";
                }

                var incidentQuery = 'assigned_to=' + this.currentUser.sysId + '^ORcaller_id=' + this.currentUser.sysId;
                var requestQuery = 'requested_for=' + this.currentUser.sysId;
                var changeQuery = 'requested_by=' + this.currentUser.sysId;
                var fields = 'number,short_description,state,priority';

                var incidents = await this.makeServiceNowRequest('incident', incidentQuery, fields);
                var requests = await this.makeServiceNowRequest('sc_request', requestQuery, fields);
                var changes = await this.makeServiceNowRequest('change_request', changeQuery, fields);

                var responseText = ']]>🎫<![CDATA[ Here are your recent tickets';
                if (this.currentUser.firstName) {
                    responseText += ', ' + this.currentUser.firstName;
                }
                responseText += ':\n\n';
                
                if (incidents.result) {
                    if (incidents.result.length > 0) {
                        responseText += "]]>📋<![CDATA[ **Incidents:**\n";
                        var incidentsList = incidents.result;
                        for (var i in incidentsList) {
                            var inc = incidentsList[i];
                            responseText += '• ' + inc.number + ': ' + inc.short_description + ' (' + this.getStateText(inc.state) + ')\n';
                        }
                        responseText += "\n";
                    }
                }
                
                if (requests.result) {
                    if (requests.result.length > 0) {
                        responseText += "]]>📝<![CDATA[ **Service Requests:**\n";
                        var requestsList = requests.result;
                        for (var i in requestsList) {
                            var req = requestsList[i];
                            responseText += '• ' + req.number + ': ' + req.short_description + ' (' + this.getStateText(req.state) + ')\n';
                        }
                        responseText += "\n";
                    }
                }
                
                if (changes.result) {
                    if (changes.result.length > 0) {
                        responseText += "]]>🔄<![CDATA[ **Change Requests:**\n";
                        var changesList = changes.result;
                        for (var i in changesList) {
                            var chg = changesList[i];
                            responseText += '• ' + chg.number + ': ' + chg.short_description + ' (' + this.getStateText(chg.state) + ')\n';
                        }
                    }
                }
                
                var hasIncidents = incidents.result ? incidents.result.length > 0 : false;
                var hasRequests = requests.result ? requests.result.length > 0 : false;
                var hasChanges = changes.result ? changes.result.length > 0 : false;
                
                if (!hasIncidents) {
                    if (!hasRequests) {
                        if (!hasChanges) {
                            responseText = "✅ No active tickets found for your account. Great job staying on top of things!";
                        }
                    }
                }
                
                return responseText;
            } catch (error) {
                return '❌ Sorry, I could not retrieve your ticket information. Error: ' + error.message;
            }
        }

        getStateText(stateValue) {
            var stateMap = {
                '1': ']]>🆕<![CDATA[ New', '2': ']]>🔄<![CDATA[ In Progress', '3': '⏸️ On Hold', '6': '✅ Resolved', '7': ']]>🔒<![CDATA[ Closed',
                '10': '❌ Canceled', '18': '⏳ Awaiting User Info', '19': ']]>📄<![CDATA[ Awaiting Evidence',
                '20': ']]>🔍<![CDATA[ Awaiting Problem', '21': ']]>🏢<![CDATA[ Awaiting Vendor'
            };
            return stateMap[stateValue] || (']]>📊<![CDATA[ State ' + stateValue);
        }

        async handleKnowledgeQuery(command) {
            try {
                var searchTerms = command.replace(/knowledge|article|search|find|show/gi, '').trim();
                var query = 'workflow_state=published';
                
                if (searchTerms) {
                    query += '^short_descriptionLIKE' + searchTerms + '^ORtextLIKE' + searchTerms;
                }
                
                var response = await this.makeServiceNowRequest('kb_knowledge', query, 'number,short_description,text,kb_category.label');
                
                if (response.result) {
                    if (response.result.length > 0) {
                        var responseText = ']]>📚<![CDATA[ Here are relevant knowledge articles';
                        if (this.currentUser.firstName) {
                            responseText += ' for you, ' + this.currentUser.firstName;
                        }
                        responseText += ':\n\n';
                        
                        var articlesList = response.result;
                        var articleIndex = 0;
                        for (var i in articlesList) {
                            var article = articlesList[i];
                            articleIndex++;
                            responseText += articleIndex + '. ]]>📖<![CDATA[ **' + article.short_description + '** (' + article.number + ')\n';
                            if (article['kb_category.label']) {
                                responseText += '   ]]>🏷<![CDATA[️ Category: ' + article['kb_category.label'] + '\n';
                            }
                            var preview = article.text ? article.text.substring(0, 100) + '...' : 'No preview available';
                            responseText += '   ]]>💡<![CDATA[ ' + preview + '\n\n';
                        }
                        return responseText;
                    }
                }
                
                if (searchTerms) {
                    return ']]>🔍<![CDATA[ No knowledge articles found matching "' + searchTerms + '". Try using different keywords.';
                } else {
                    return "]]>📚<![CDATA[ No published knowledge articles found. Please contact your IT support team.";
                }
            } catch (error) {
                return '❌ Sorry, I could not search the knowledge base. Error: ' + error.message;
            }
        }

        async handleCatalogQuery(command) {
            try {
                var response = await this.makeServiceNowRequest('sc_cat_item', 'active=true^available_for=desktop', 'name,short_description,category.title,sys_id');
                
                if (response.result) {
                    if (response.result.length > 0) {
                        var responseText = ']]>📋<![CDATA[ Here are available service catalog items';
                        if (this.currentUser.firstName) {
                            responseText += ', ' + this.currentUser.firstName;
                        }
                        responseText += ':\n\n';
                        
                        var categorized = {};
                        var catalogItems = response.result;
                        for (var i in catalogItems) {
                            var item = catalogItems[i];
                            var category = item['category.title'] || 'General';
                            if (!categorized[category]) {
                                categorized[category] = [];
                            }
                            categorized[category].push(item);
                        }
                        
                        for (var category in categorized) {
                            responseText += ']]>📁<![CDATA[ **' + category + ':**\n';
                            var items = categorized[category];
                            for (var j in items) {
                                var item = items[j];
                                responseText += '• ⚡ ' + item.name + '\n';
                                if (item.short_description) {
                                    responseText += '  ]]>💬<![CDATA[ ' + item.short_description + '\n';
                                }
                            }
                            responseText += "\n";
                        }
                        
                        responseText += "✨ To request a service, please visit your ServiceNow portal or tell me which specific service you need.";
                        return responseText;
                    }
                }
                return "]]>📋<![CDATA[ No service catalog items are currently available. Please contact your IT support team.";
            } catch (error) {
                return '❌ Sorry, I could not retrieve the service catalog. Error: ' + error.message;
            }
        }

        async handleIncidentCreation(command) {
            try {
                if (!this.currentUser.sysId) {
                    return "]]>🚨<![CDATA[ I can help you create an incident, but I need proper user context. Please ensure you're logged into ServiceNow properly, or contact your system administrator.";
                }

                var description = command.replace(/create|incident|report|new|issue/gi, '').trim();
                
                var descriptionTooShort = true;
                var minLength = 10;
                if (description.length === minLength || description.length > minLength) {
                    descriptionTooShort = false;
                }
                
                if (!description || descriptionTooShort) {
                    var greeting = this.currentUser.firstName ? this.currentUser.firstName : 'there';
                    return ']]>👋<![CDATA[ ' + greeting + ', I can help you create a new incident. Please provide more details about the issue you are experiencing, such as: "Create incident for laptop not connecting to WiFi" or "Report issue with email not working".';
                }
                
                var shortDesc = description;
                var descriptionTooLong = (description.length > 160);
                if (descriptionTooLong) {
                    shortDesc = description.substring(0, 160);
                }
                
                var incidentData = {
                    short_description: shortDesc,
                    description: description,
                    caller_id: this.currentUser.sysId,
                    category: 'inquiry',
                    subcategory: 'other',
                    priority: '3',
                    impact: '3',
                    urgency: '3',
                    state: '1'
                };
                
                var response = await this.makeServiceNowRequest('incident', '', '', 'POST', incidentData);
                
                if (response.result) {
                    var greeting = this.currentUser.firstName ? this.currentUser.firstName : 'User';
                    return '✅ Incident created successfully, ' + greeting + '!\n\n]]>🎫<![CDATA[ **Ticket Number:** ' + response.result.number + '\n]]>📝<![CDATA[ **Description:** ' + response.result.short_description + '\n]]>📊<![CDATA[ **Status:** New\n\n]]>🔔<![CDATA[ You will receive updates as we work on resolving this issue.';
                } else {
                    return "❌ There was an issue creating the incident. Please try again or contact IT support directly.";
                }
            } catch (error) {
                return '❌ Sorry, I could not create the incident. Error: ' + error.message + '. Please contact your IT support team directly.';
            }
        }

        async handleUserProfile(command) {
            try {
                if (!this.currentUser.sysId) {
                    return "]]>👤<![CDATA[ I need proper user context to show profile information. Please ensure you're logged into ServiceNow properly.";
                }

                var response = await this.makeServiceNowRequest('sys_user', 'sys_id=' + this.currentUser.sysId, 'name,email,department.name,title,last_login_time,active,phone,location.name');
                
                if (response.result) {
                    if (response.result.length > 0) {
                        var user = response.result[0];
                        var greeting = this.currentUser.firstName ? this.currentUser.firstName : 'User';
                        var responseText = ']]>👤<![CDATA[ Here is your profile information, ' + greeting + ':\n\n';
                        responseText += ']]>👨<![CDATA[‍]]>💼<![CDATA[ **Name:** ' + user.name + '\n';
                        responseText += ']]>🔐<![CDATA[ **Username:** ' + this.currentUser.userName + '\n';
                        responseText += ']]>📧<![CDATA[ **Email:** ' + user.email + '\n';
                        if (user['department.name']) {
                            responseText += ']]>🏢<![CDATA[ **Department:** ' + user['department.name'] + '\n';
                        }
                        if (user.title) {
                            responseText += ']]>💼<![CDATA[ **Title:** ' + user.title + '\n';
                        }
                        if (user.phone) {
                            responseText += ']]>📞<![CDATA[ **Phone:** ' + user.phone + '\n';
                        }
                        if (user['location.name']) {
                            responseText += ']]>📍<![CDATA[ **Location:** ' + user['location.name'] + '\n';
                        }
                        if (user.last_login_time) {
                            var lastLogin = new Date(user.last_login_time).toLocaleString();
                            responseText += ']]>🕐<![CDATA[ **Last Login:** ' + lastLogin + '\n';
                        }
                        var isActive = (user.active === 'true') ? '✅ Active' : '❌ Inactive';
                        responseText += ']]>📊<![CDATA[ **Account Status:** ' + isActive + '\n\n';
                        responseText += "⚙️ To update your profile information, please contact your system administrator or visit the Employee Center in ServiceNow.";
                        return responseText;
                    }
                }
                
                var greeting = this.currentUser.firstName ? this.currentUser.firstName : 'User';
                var responseText = ']]>👤<![CDATA[ Here is your basic profile information, ' + greeting + ':\n\n';
                responseText += ']]>👨<![CDATA[‍]]>💼<![CDATA[ **Name:** ' + (this.currentUser.firstName || 'Unknown') + ' ' + (this.currentUser.lastName || '') + '\n';
                responseText += ']]>🔐<![CDATA[ **Username:** ' + (this.currentUser.userName || 'Unknown') + '\n';
                if (this.currentUser.email) {
                    responseText += ']]>📧<![CDATA[ **Email:** ' + this.currentUser.email + '\n';
                }
                responseText += "\n]]>📋<![CDATA[ For complete profile details, please visit your Employee Center in ServiceNow.";
                return responseText;
            } catch (error) {
                var greeting = this.currentUser.firstName ? this.currentUser.firstName : 'User';
                var responseText = ']]>👤<![CDATA[ Here is your basic profile information, ' + greeting + ':\n\n';
                responseText += ']]>👨<![CDATA[‍]]>💼<![CDATA[ **Name:** ' + (this.currentUser.firstName || 'Unknown') + ' ' + (this.currentUser.lastName || '') + '\n';
                responseText += ']]>🔐<![CDATA[ **Username:** ' + (this.currentUser.userName || 'Unknown') + '\n';
                if (this.currentUser.email) {
                    responseText += ']]>📧<![CDATA[ **Email:** ' + this.currentUser.email + '\n';
                }
                responseText += '\n❌ Error retrieving detailed profile: ' + error.message;
                return responseText;
            }
        }

        speakResponse(text) {
            if ('speechSynthesis' in window) {
                var utterance = new SpeechSynthesisUtterance(text);
                utterance.rate = 0.9;
                utterance.pitch = 1;
                utterance.volume = 0.8;
                speechSynthesis.speak(utterance);
            }
        }

        storeConversation(message, role) {
            var conversationEntry = {
                timestamp: new Date().toISOString(),
                role: role,
                message: message,
                sessionId: this.getSessionId()
            };
            
            this.conversationHistory.push(conversationEntry);
            console.log('Stored conversation entry:', conversationEntry);
            
            if (this.currentUser.sysId) {
                this.storeToServiceNow(conversationEntry);
            }
        }

        async storeToServiceNow(entry) {
            try {
                if (!this.currentUser.sysId) {
                    console.warn('Cannot store conversation - user context not available');
                    return;
                }

                var storeData = {
                    timestamp: entry.timestamp,
                    role: entry.role,
                    message: entry.message,
                    session_id: entry.sessionId,
                    user_sys_id: this.currentUser.sysId,
                    user_name: this.currentUser.userName,
                    user_email: this.currentUser.email
                };
                
                var response = await this.makeServiceNowRequest('x_941497_voice_ast_va', '', '', 'POST', storeData);
                
                if (response.result) {
                    console.log('Successfully stored conversation entry to ServiceNow:', response.result.sys_id);
                }
            } catch (error) {
                console.error('Failed to store conversation entry to ServiceNow:', error);
            }
        }

        getSessionId() {
            if (!this.sessionId) {
                this.sessionId = 'session_' + Date.now() + '_' + Math.random().toString(36).substr(2, 9);
            }
            return this.sessionId;
        }

        handleError(errorMessage) {
            var chatContainer = document.getElementById('chatContainer');
            var errorDiv = document.createElement('div');
            errorDiv.className = 'error';
            errorDiv.textContent = errorMessage;
            chatContainer.appendChild(errorDiv);
            chatContainer.scrollTop = chatContainer.scrollHeight;
            
            this.updateStatus('❌ Error occurred. Please try again.', 'error');
        }
    }

    function handleQuickAction(actionType) {
        var assistant = window.voiceAssistant;
        if (assistant) {
            var command = '';
            if (actionType === 'ticket') {
                command = 'Show me my ticket status';
            }
            if (actionType === 'knowledge') {
                command = 'Search knowledge base articles';
            }
            if (actionType === 'catalog') {
                command = 'Show me the service catalog';
            }
            if (actionType === 'incident') {
                command = 'Create a new incident';
            }
            assistant.processVoiceCommand(command);
        }
    }

    function toggleTheme() {
        // Theme toggle functionality can be implemented here
        var themeToggle = document.querySelector('.theme-toggle');
        if (themeToggle.textContent === ']]>🌙<![CDATA[') {
            themeToggle.textContent = '☀️';
            // Add dark theme styles
        } else {
            themeToggle.textContent = ']]>🌙<![CDATA[';
            // Remove dark theme styles
        }
    }

    window.addEventListener('DOMContentLoaded', function() {
        window.voiceAssistant = new ServiceNowVoiceAssistant();
    });
</script> ]]></html>
        <name>Voice Assistant</name>
        <processing_script/>
        <sys_class_name>sys_ui_page</sys_class_name>
        <sys_created_by>vasanth</sys_created_by>
        <sys_created_on>2025-07-29 10:35:51</sys_created_on>
        <sys_id>1a45fe115347e21063a317c0a0490e3c</sys_id>
        <sys_mod_count>3</sys_mod_count>
        <sys_name>Voice Assistant</sys_name>
        <sys_package display_value="Voice assistant Conversation" source="x_941497_voice_ast">5202b25953c3e21063a317c0a0490ea4</sys_package>
        <sys_policy/>
        <sys_scope display_value="Voice assistant Conversation">5202b25953c3e21063a317c0a0490ea4</sys_scope>
        <sys_update_name>sys_ui_page_1a45fe115347e21063a317c0a0490e3c</sys_update_name>
        <sys_updated_by>vasanth</sys_updated_by>
        <sys_updated_on>2025-07-29 11:18:12</sys_updated_on>
    </sys_ui_page>
</record_update>
